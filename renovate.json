{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": ["config:recommended", "group:allNonMajor"],
    "timezone": "Asia/Tokyo",
    "packageRules": [
        {
            "description": "eslint-plugin-react (pulled in by eslint-config-next) does not support ESLint 10 yet; it still calls context.getFilename(), removed in v10. Hold eslint on 9.x until upstream ships support.",
            "matchPackageNames": ["eslint"],
            "allowedVersions": "<10"
        }
    ]
}
